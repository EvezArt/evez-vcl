services:
  - type: web
    name: evez-vcl
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn vcl_server:app --host 0.0.0.0 --port $PORT
    plan: free
